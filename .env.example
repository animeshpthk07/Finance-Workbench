# Documentation only: this file is not loaded automatically.
# Set these through your shell or Streamlit secrets (never commit real keys).
OPENAI_API_KEY=
FINANCE_WORKBENCH_AI_MODEL=
# Set true for an anonymous public demo: disables uploads and OpenAI calls.
FINANCE_WORKBENCH_PUBLIC_DEMO=false
